<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.spring.cafealley.board.mapper.IBoardMapper">
  
  <insert id="regist">
  	INSERT INTO noboard(bno, title, writer, content)
	VALUES(Noboard_seq.NEXTVAL, #{title}, #{writer}, #{content})
  </insert>
  
  <select id="getContent" resultType="com.spring.cafealley.command.BoardVO">
		SELECT * FROM noboard
		WHERE bno = #{bno}
	</select>
	
	<update id="update">
		UPDATE noboard
		SET title = #{title}, content = #{content}, regdate = sysdate, ismod = '1'
		WHERE bno = #{bno}
	</update>
	
	<delete id="delete">
		DELETE FROM noboard
		WHERE bno = #{bno}
	</delete>
  
  
  
  </mapper>